<% include ../partials/header %>
    <link href='https://fonts.googleapis.com/css?family=Arbutus Slab' rel='stylesheet'>

<style>
    .bg {
        background: rgba(32, 134, 107, 0.8);
        overflow: hidden; 
        min-height: 300px;
        position: relative;
        padding-top: 90px;
    }
</style>
    <% if(currentUser){ %>
        <section class="text-white bg text-center">
            <div class="container">
                <h1 class="display-4">User Dashboard</h1>
                <p class="lead">Manage your Book SMart account</p>
            </div>
        </section>

        <!-- Breadcrumb -->
        <section id="bc" class="mt-3">
            <div class="container">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href="/" class="text-muted">
                            <i class="fas fa-home"></i> Home
                        </a>
                    </li>
                    <li class="breadcrumb-item active"> User Dashboard</li>
                </ol>
            </div>
        </section>

        <section id="dashboard" class="py-4">
            <div class="container">
                <h2>Welcome <span><%= currentUser.name %></span></h2>
                <p>Here are the books that you have posted about</p>
                <p>Email: <%= currentUser.email %></p>
                <p><%= currentUser.date %></p>
            </div>
        </section>

        <!--
        <div class="container">
            <div class="row">
                <% //if(currentUser && profile.author.id.equals(currentUser._id)){ %>
                    <% //for(var i = 0; i < profile.length; i++){ %>
                    <div class="col-md-3 col-sm-12 col-12 my-3">
                        <a href="/sell" class="a">
                            <div class="hover">
                                <div class="card">
                                    <img class="card-img-top" src="<%= //profile[i].image %>" height="320px">
                                    <div class="card-body">
                                        <h6><%= //profile[i].title %></h6>
                                        <p>Price: 
                                        <%= //profile[i].sellingPrice %></p>
                                        <p>Original Price:
                                            <%= //profile[i].originalPrice %></p>
                                        <!--
                                        <a class="btn btn-block btn-success" href="/sell/<%= //profile[i]._id %>">
                                            See More
                                        </a>
                                        -->
                                    </div>
                                </div>
                            </div>
                        </a>
                    </div>
                    <% //} %>
                <% //} %>
            </div>
        </div>
        -->

    <% } %>
<% include ../partials/footer %>