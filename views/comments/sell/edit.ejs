<% include ../../partials/header %>

<div class="container my-5">
    <div class="col-md-6 mx-auto">
        <div class="card p-4 mt-2 shadow">
            <h5 class="bg-light my-3 px-3 py-2">Update Comment</h5>
            <form action="/sell/<%= sell_id %>/comments/<%= comment._id %>?_method=PUT" method="post">
                <div class="form-group">
                    <input class="form-control" type="text" value="<%= comment.text %>" name="commentE[text]">
                </div>
                <div class="form-group">
                    <button class="btn btn-info btn-block">Submit</button>
                </div>
                <a class="btn btn-light" href="/sell">Go to Sell page</a>
            </form>
        </div>
    </div>
</div>

<% include ../../partials/footer %>